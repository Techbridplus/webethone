/**
 * @fileoverview Configuration file for Next.js application.
 * @type {import('next').NextConfig}
 */
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com'],
    },
};

export default nextConfig;
